# Environment Variables for application <%= @application %>

<% @environment.each do | key, value | %>
    <%= key %>=<%= value %>
<% end %>

<% @env.each do | key, value | %>
    <%= key %>=<%= value %>
<% end %>
